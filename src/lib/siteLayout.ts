/**
 * Largura global do conteúdo: ~90% da viewport, centralizado.
 * Use em headers, mains e seções full-bleed que precisam alinhar ao mesmo grid.
 */
export const siteContentWidthClass =
  "mx-auto box-border w-[90%] max-w-full";
